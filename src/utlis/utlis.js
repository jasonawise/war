/**
 * This function shuffles and deals a deck of cards
 * @param {object} deck The deck of cards
 * @param {number} numberPlayers the number of players in the game
 * @return
 */
export const dealCards = (deck, numberPlayers) => {
  const numberOfHandsToDeal = numberPlayers
}
